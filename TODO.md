# 競馬予想AI 開発ロードマップ & 改善計画書 (ROADMAP.md)

本ドキュメントは、2024〜2026年データの蓄積完了以降に実施する「モデルの再学習」「ベッティング最適化」「特徴量高度化」「モンテカルロシミュレータ構築」「週末完全自動運用」の作業フェーズと実装手順を定義する計画書である。

---

## 1. 全体ロードマップ概要

```text
[Step 1: 基盤強化]
  └─ 2024〜2026年データでのモデル再学習 (main_phase2.py)
  └─ テスト期間におけるベッティング戦略の最終確定 (optimize_betting.py)
        │
[Step 2: 特徴量の高度化 (精度85〜90点化)]
  └─ タイム指数の馬場補正（同日同コース平均偏差値）
  └─ コース×枠順好走率 & 先行馬頭数によるペース予測
        │
[Step 3: モンテカルロ・レースシミュレータ構築]
  └─ 1万回仮想出走エンジンの実装 (走破タイム分散モデル)
  └─ LightGBM × 物理シミュレーションのダブルチェック判定
  └─ ワイド・馬連・3連複への期待値計算拡張
        │
[Step 4: 週末完全自動運用]
  └─ 当日メイン・全レースの一括推論・Discordリッチ通知
  └─ タスクスケジューラ / 定時実行バッチによる無人運用
```

---

## 2. 各フェーズの詳細実装計画

### フェーズ 1: 大容量データでのモデル更新 & ベッティング最適化

1. **モデルの再学習**
   * 直近3年分（約10万件規模）のデータセットで学習を実行し、モデルファイル（`models_saved/lgbm_model.txt`）を更新。
   ```powershell
   python main_phase2.py
   ```
2. **黄金ルールの最終抽出**
   * 3年分のテストデータに対しグリッドサーチを実行。
   ```powershell
   python optimize_betting.py
   ```
3. **推論ロジックへの反映**
   * 得られた最適パラメータ（EV閾値、複勝率下限、オッズ帯、順位条件）を `predict.py` 内の判定部に更新。

---

### フェーズ 2: ドメイン特徴量の高度化（精度向上）

既存のDB生データ（追加スクレイピング不要）を活用し、以下の高付加価値特徴量を `src/features/` に追加実装する。

* **馬場差補正スピード指数（タイム指数）**
  * 各レースの走破タイムを「同日・同競馬場・同コース種別・同距離の良馬場換算平均タイム」と比較し、各馬の走破偏差値を算出。
* **コース×枠順バイアス値**
  * 競馬場・距離・枠番ごとの過去好走率（複勝率）を統計量として算出（例: 東京ダ1600mの外枠補正値）。
* **展開・ペース予測指標**
  * 過去の通過順位から「前走・前々走で1コーナーを3番手以内で通過した馬の頭数」をカウントし、対象レースがハイペースかスローペースかを定量化。
* **馬体重変動率**
  * `(horse_weight_diff / horse_weight) * 100` による前走比の体重変動割合。

---

### フェーズ 3: モンテカルロ・レースシミュレータの構築

機械学習の出力確率を物理的シミュレーションで裏付ける二重検証システムを構築する。

* **新規モジュール**: `src/simulation/race_simulator.py`
  * **走破タイム分布モデル**: 各馬の「平均走破タイム」と「ばらつき（標準偏差 $\sigma$）」を算出し、正規乱数により仮想レースを 10,000 回実行。
  * **着順確率の導出**: 1万回の試行から、各馬の 1着率・2着以内率・3着以内率、および馬連・ワイド・3連複の組み合わせ成立確率を集計。
* **買い目判定のダブルフィルター**
  * 「LightGBMによるEV基準を満たす」かつ「モンテカルロシミュレーションでも勝率上位」の馬のみを厳選推奨に採用。

---

### フェーズ 4: 週末完全自動運用基盤の確立

* **一括推論・通知スクリプトの活用**
  ```powershell
  # 当日のメインレース（11R）を一括推論
  python run_daily_predict.py

  # 当日の後半戦（9〜12R）を一括推論
  python run_daily_predict.py --rounds 9,10,11,12
  ```
* **タスクスケジューラ登録（定時実行）**
  * 毎週土曜・日曜の発走前（例: 朝 9:30 または 14:00）に自動起動するバッチファイル（`run_today.bat`）を配備。

---

## 3. 次回作業チェックリスト

- [ ] 2024〜2026年のデータ収集完了確認
- [ ] `main_phase2.py` 実行によるモデル再生成
- [ ] `optimize_betting.py` 実行による最新ベッティング条件の抽出
- [ ] `predict.py` 買い目判定パラメータの更新
- [ ] タイム指数・展開特徴量のコーディング（フェーズ2）
- [ ] モンテカルロシミュレータの作成（フェーズ3）